const GrocList = require("./GrocList/main.js")

// TODO: Add configuration to GrocList
/*
GrocList.init({
  storage: false,
  // storage: {
  //   database: "storage.db" 
  // },
})
*/
GrocList.start()
